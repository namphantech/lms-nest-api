export enum UserStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}
export enum Roles {
  ADMIN = 'ADMIN',
  USER = 'USER',
}
export enum Status {
  PENDING = 'PENDING',
  COMPLETE = 'COMPLETE',
}
export enum ResetPassword {
  SUBJECT = 'Reset password!',
}
export enum ProductVariantType {
  COLOR = 'COLOR',
  SIZE = 'SIZE',
}
